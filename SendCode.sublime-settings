{
	// "prog": "r",
	// "r":
	// {
	// 	"prog": "r"
	// }
	"prog": "terminal",

	"r" : {
        "prog": "r",
        // "prog": "terminal",
        // turn bracketed_paste_mode on if radian or readline 7.0 is in use
        "bracketed_paste_mode": false
    },

    "rmd" : {
        "prog": "rstudio",
        // turn bracketed_paste_mode on if radian or readline 7.0 is in use
        "bracketed_paste_mode": false
    }
}
